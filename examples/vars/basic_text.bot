"""
Basic text live variable example

Display unicode text and manipulate the font size.
"""

size(400, 300)
colorrange(255)

var("txt", TEXT, "test")
var("font_size", NUMBER, 100., 10., 200.)

background(80, 80, 80)
fontsize(font_size)
text(txt, 10, HEIGHT - 30)
