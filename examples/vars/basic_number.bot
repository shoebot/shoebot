"""
Basic number live variable example
"""

size(300, 300)
colorrange(255)
colormode(HSB)

var('hue', NUMBER, 0)
var('saturation', NUMBER)
var('brightness', NUMBER, 96)
var('show', BOOLEAN, False)

background(hue, saturation, brightness)
if show:
    fill(255)
    ellipse(100, 100, 100, 100)
